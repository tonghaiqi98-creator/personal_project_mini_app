# 项目开发助手说明

本文件用于兼容读取小写 `agent.md` 的工具或上传流程。

完整开发规则请以项目根目录下的 `AGENTS.md` 为准。

当前项目是「轻量堂食扫码点餐微信小程序 Demo」，开发时需要遵守以下边界：

1. 使用微信原生小程序、WXML、WXSS、JavaScript。
2. 当前阶段优先使用本地 Mock 数据。
3. 未明确要求时，不接入 CloudBase、真实微信支付、数据库或第三方 UI 库。
4. 不主动扩大 MVP 范围。
5. 每次改动保持小步、清晰、可验证。

